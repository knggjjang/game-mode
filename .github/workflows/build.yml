name: Build Game Mode

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Cache Node modules
        uses: actions/cache@v3
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: npm ci

      - name: Build Next.js
        run: npm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v3
        with:
          name: frontend-build
          path: out/
          retention-days: 5

  # NOTE: Tauri Windows build requires Windows runner and full toolchain
  # For now, we recommend building locally or setting up a separate Windows CI
  # Uncomment and configure when ready to build Windows binaries
  # build-tauri-windows:
  #   runs-on: windows-latest
  #   needs: build-frontend
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   steps:
  #     - uses: actions/checkout@v3
  #     - uses: actions/setup-node@v3
  #       with:
  #         node-version: '18'
  #     - uses: dtolnay/rust-toolchain@stable
  #       with:
  #         targets: x86_64-pc-windows-msvc
  #     - uses: Swatinem/rust-cache@v2
  #       with:
  #         workspaces: './src-tauri'
  #     - run: npm ci
  #     - run: npm run tauri build
  #       env:
  #         TAURI_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
  #         TAURI_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}
